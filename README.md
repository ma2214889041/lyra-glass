# Lyra 眼镜摄影工坊 (Lyra Eyewear Studio)

![Lyra Studio](./public/favicon.svg)

基于 Google Gemini 2.0 FlashMultimodal 模型的智能眼镜摄影与设计工坊。通过 AI 技术，为眼镜产品生成高质量的商业摄影图、试戴效果图，并提供创意设计生成功能。

## ✨ 核心功能

### 1. 创作工坊 (Dashboard)
- **上传资产**：支持上传眼镜产品 PNG/JPG 图片。
- **物理光影锁定**：保持眼镜主体的物理真实感，智能融合背景光影。
- **多场景生成**：
  - **商业模特试戴**：一键生成真人模特佩戴效果，支持自定义模特特征（性别、地域、发色等）。
  - **大师模板套用**：一键应用精心设计的摄影模板，获得品牌级视觉效果。
- **异步任务队列**：支持后台生成任务，无需等待，实时查看进度。

### 2. 模板广场 (Template Gallery)
- **精选模板库**：提供多种风格的摄影模板（极简、街头、科技感等）。
- **移动端适配**：优化的移动端浏览体验，支持横向滚动筛选。
- **一键应用**：点击模板直接进入创作模式。

### 3. 作品集与社区 (Gallery & Community)
- **个人作品集**：保存用户的生成历史，支持查看 Prompt、下载、删除。
- **社区画廊**：
  - **隐私控制**：生成内容默认私有，可一键分享至社区。
  - **Prompt 透明化**：支持查看社区作品的生成提示词，激发创作灵感。
- **响应式设计**：完美适配桌面端和移动端浏览。

## 🛠️ 技术栈

### 前端 (Frontend)
- **框架**：React 18 + Vite
- **UI 库**：Tailwind CSS (响应式设计, Glassmorphism 风格)
- **路由**：React Router v6
- **图标**：Heroicons + 自定义 SVG

### 后端 (Backend)
- **平台**：Cloudflare Workers (Serverless)
- **数据库**：Cloudflare D1 (SQLite at Edge)
- **对象存储**：Cloudflare R2 (图片存储)
- **AI模型**：Google Gemini 2.0 Flash (via `@google/genai`)

## 🚀 快速开始

### 前置要求
- Node.js >= 18.0.0
- Cloudflare 账号 (用于部署 Workers 和 D1/R2)
- Google Gemini API Key

### 安装与运行

1. **克隆项目**
   ```bash
   git clone https://github.com/ma2214889041/lyra-glass.git
   cd lyra-glass
   ```

2. **安装依赖**
   ```bash
   # 安装前端依赖
   npm install

   # 安装后端依赖
   cd workers
   npm install
   cd ..
   ```

3. **启动开发服务器**

   您需要打开两个终端分别启动前端和后端：

   **终端 1 (Backend - Workers):**
   ```bash
   cd workers
   npx wrangler dev
   # 运行在 http://127.0.0.1:8787
   ```

   **终端 2 (Frontend - Vite):**
   ```bash
   npm run dev
   # 运行在 http://localhost:5173
   ```

## 📦 部署指南

### Cloudflare Workers (后端)
```bash
cd workers
npx wrangler deploy
```

### Static Web (前端)
```bash
npm run build
# 将 dist/ 目录部署到 Cloudflare Pages 或 Vercel
```

## 📄 许可证
MIT License
